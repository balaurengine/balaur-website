---
description: "Rollback between two engines that exchange only inputs, a digest per tick to catch a desync, and one Transport trait over WebSockets and QUIC."
slug: rollback-networking
title: Rollback networking
authors: [dragos]
image: /img/manual/networking_faults.png
keywords: [rollback netcode, deterministic lockstep, quic, websocket, multiplayer game engine]
---

import Clip from '@site/src/components/Clip';

Two engines can run the same game in rollback, exchanging only inputs. A
digest per tick catches a desync, and one `Transport` trait covers
WebSockets and QUIC.

{/* truncate */}

<Clip name="determinism_replay" alt="Play records; the bird flies; stop. The session opens from the Session dock and the bird flies again with nobody at the mouse" />

- A snapshot restores a tick and the deterministic simulation re-runs the
  same inputs. Node ids are stable across spawns and re-runs, and scripts
  see `rollback::input` and `rollback::is_resimulating`.
- A peer publishes a tick's hash once every player's input for it has
  arrived. A desync names the tick.
- `Transport` runs over WebSockets, and over QUIC datagrams through
  `balaur_webtransport`, where a lost input costs a misprediction.
  `balaur_http`, `balaur_websocket` and `balaur_webtransport` are one crate
  each, behind their own features.
- Delay, jitter and datagram loss can be set on every session link, from
  the editor's settings.

  ![The Netcode page of the settings screen: faults, delay, jitter and loss](/img/manual/networking_faults.webp)

## Next

- A session a script can open, and replication:
  [PLAN-networking.md](https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md).
  Rollback runs between two engines in a test and over a socket.
- No raw UDP. QUIC datagrams cover it, with encryption and in the browser.

[Networking](/docs/manual/networking) · [Determinism](/docs/manual/determinism) · [Balaur for multiplayer](/multiplayer)
