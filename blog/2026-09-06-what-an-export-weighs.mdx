---
description: "balaur export reports what a pack weighs, drops unreferenced assets, recompresses images, subsets fonts and rewrites WAV as FLAC."
slug: what-an-export-weighs
title: Smaller exports
authors: [dragos]
image: /img/manual/export_size.png
keywords: [export size, pack size, texture atlas, font subsetting, webp, game build size]
---

`balaur export` reports what a pack weighs, drops unreferenced assets,
re-encodes PNGs, subsets fonts and rewrites WAV as FLAC. All of it lossless.

{/* truncate */}

The editor's own pack, 2.1 MB: fonts 77%, scripts 22%, images 1%. The web
runtime beside it was 17.1 MB.

## What landed

- **A report.** `balaur export` prints the pack by section, by extension, its
  ten heaviest entries, and the assets nothing references. `--report` prints
  without writing.
- **Strip.** `strip = true` drops every asset no scene, asset document,
  script string literal or `id://` reference names. Off by default.
- **Images.** `images = "smallest"` keeps the smaller of the PNG optimiser
  and lossless WebP. A 128x256 sprite goes 1.1 KB to 0.5 KB. JPEGs are left
  alone.
- **Fonts.** `fonts = "subset"` keeps the code points in the project's scenes
  and scripts, printable ASCII, and any `font_ranges` named for server text.
  One face: 421 KB to 29 KB. Kerning, ligatures and Arabic joining survive.
- **Audio.** `audio = "flac"`: WAV as FLAC, lossless, about half the size.
- **Lossy.** `images = "quantised"` with `images_quality`, `audio = "vorbis"`
  with `audio_quality`. Separate keys from the lossless modes.
- **`recode`.** In a picture's import settings, it overrides the export's
  mode for that file.
- **Bitmap fonts.** `.fnt` descriptors are in the pack's extension list.

Every mode keeps an image's pixel dimensions.

```toml
[export]
strip = true                # drop an asset no scene, script or keep glob names
keep = ["sfx/**"]           # ...except paths a script builds at run time
images = "smallest"         # recompress PNG, or write lossless WebP, whichever wins
fonts = "subset"            # cut a face to the characters the project shows
font_keep = ["fonts/ui-*"]  # ...but ship these whole
audio = "flac"              # write uncompressed WAV as lossless FLAC
```

## Not size levers

- Atlases: a few percent of the image bytes, and no draw-call win; the
  renderer issues one call per node.
- GPU-compressed textures (BC, ETC2, ASTC): a byte per pixel on disk, about
  twelve times the PNG, but they cut video memory and upload time.
- Compressing the pack: the media inside is compressed, and static hosts
  gzip on the way out.

[Build size](/docs/manual/build-size) is the manual page.
