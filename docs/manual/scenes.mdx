---
sidebar_position: 1
---

# Scenes and nodes

## Nodes are entities

A node is nothing but an ECS entity with `Name`, `Parent`, `Children`,
`Transform`, and `GlobalTransform` components. Node paths (`"A/B/C"`),
transform propagation, and recursive free are core systems. Plugins hang
their own components off the same entities, so "node" is an API surface, not
a data structure with a cost.

Scripts reach the tree through the node API — 30 operations declared once in
the core and available in every language: `get_node`, `parent`, `children`,
`add_child`, `set_parent` (which keeps the node where it is in the world),
`queue_free`, `position` / `set_position`, `rotation_euler`, `scale`,
`translate`, `attach_script`, the component operations, and the rest. Node
paths accept `..`, so `../Target` names a sibling. The full list is in the
[script API reference](../reference/script-api.md).

Structural changes requested by scripts (`queue_free`) are deferred to the
end of the frame, so iteration is never invalidated mid-frame.

## Scene files

A scene is declarative TOML: a flat list of `[[nodes]]` entries with `name`,
optional `parent` (by id), `position`, `script`, and any component keys the
registered plugins contribute.

```toml
[[nodes]]
id = "n_ball"
name = "Ball"
parent = "n_world"
position = [0, 6, 0]
script = "scripts/ball.luau"
body3d = "dynamic"
collider3d = { kind = "ball", radius = 0.5 }
shape3d = { kind = "ball", radius = 0.5 }
```

Scene keys are applied in plugin registration order, which is deterministic.
Scripts can instantiate scenes at run time with `scene.instantiate` and spawn
nodes with `scene.spawn`.

## Components

Plugins declare named, schema-described components. A schema gives each
property a `type` from a closed set — `float`, `bool`, `string`, `enum`,
`vec2`, `vec3`, `color`, `asset` — plus defaults, enum options, an optional
scalar shorthand (`body = "dynamic"`), and `readonly` markers the inspector
honours.
A malformed schema fails at boot, naming the component and property, rather
than at the first inspector row.

One registration buys three things at once:

- **The scene-file key.** `collider = { kind = "ball", radius = 0.5 }` works
  because the physics plugin registered `collider3d`.
- **The runtime API.** `node:set_component`, `get_component`,
  `has_component`, `remove_component`, `component_names`, plus
  `scene.component_types()` and `scene.component_schema(name)` for
  discovery.
- **Editor support.** The editor's "Add component" palette and property
  inspectors are generated from the registry, so third-party plugin
  components are addable and editable with zero editor changes.

The components registered by the standard plugins:

| Component | Plugin | Purpose |
| --- | --- | --- |
| `body3d`, `collider3d` | physics | 3D rigid body and collider (rapier3d) |
| `body2d`, `collider2d` | physics | 2D rigid body and collider (rapier2d) |
| `shape3d`, `shape2d` | render | Renderable 3D / 2D shape, with a `color` |
| `sprite`, `tilemap`, `particles` | render | Textured 2D quad (with sheets), a tile grid over a tileset, a particle emitter |
| `mesh` | render | An OBJ or glTF mesh, optionally skinned to a `skeleton` |
| `polygon` | render | A textured 2D polygon, GPU-skinned to a rig |
| `camera` | render | The 2D or 3D camera |
| `bone2d`, `bone3d` | core | A rest pose; a node with one is a bone |
| `modifier2d` | anim | `look_at` or `two_bone_ik` on a rig, run after the clip |
| `animation` | anim | A clip library (by reference or inline) and an `autoplay` clip |
| `sound` | audio | A sound file with volume and looping |
| `widget` | ui | HUD element (label / button / panel) anchored to the screen |

There is deliberately no class hierarchy behind any of this: no `Node2D`
that inherits from `Node`, no `RigidBody` subtype. A node *is* whatever its
components make it, and the generated
[component catalog](../reference/components.md) lists every registered
component with every property, default, and option.

A tagged-union property is always called `kind` (`kind = "ball"`), so `type`
always means the datatype.
