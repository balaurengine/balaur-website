---
title: "Built on — the Rust libraries under Balaur"
sidebar_label: "Built on"
description: "The Rust libraries under Balaur — hecs, Rapier, wgpu, egui, rodio, gilrs, Rune, glam and libm — and what each one does for the engine."
sidebar_position: 6
---

# Built on

Balaur reuses the Rust ecosystem rather than re-implementing it. These are
the major libraries underneath, and what each one does for the engine.

| Library | What it does in Balaur |
| --- | --- |
| [hecs](https://github.com/Ralith/hecs) | The ECS every node is an entity in |
| [Rapier](https://rapier.rs) | 2D and 3D physics, built with `enhanced-determinism` |
| [wgpu](https://wgpu.rs) via [kiss3d](https://github.com/sebcrozet/kiss3d) | Rendering, windowed and offscreen; [winit](https://github.com/rust-windowing/winit) for the window |
| [egui](https://github.com/emilk/egui) | The immediate-mode UI scripts draw, and the editor itself |
| [rodio](https://github.com/RustAudio/rodio) | Audio playback |
| [gilrs](https://gitlab.com/gilrs-project/gilrs) | Gamepads |
| [Rune](https://rune-rs.github.io/) | The scripting language and VM, [lightly forked](https://github.com/balaurengine/rune/tree/deterministic-pow) so `powf`/`powi` are deterministic |
| [glamx](https://github.com/dimforge/glamx) | Math: [glam](https://github.com/bitshifter/glam-rs) re-exported whole, plus pose and rotation types. Shared with Rapier and kiss3d so nothing converts |
| [libm](https://github.com/rust-lang/libm) | Bit-identical transcendentals on every platform, under both glam and the engine's own math |
| [gltf](https://github.com/gltf-rs/gltf), [image](https://github.com/image-rs/image) | Model and texture loading |
| [ureq](https://github.com/algesten/ureq), [tungstenite](https://github.com/snapview/tungstenite-rs), [rustls](https://github.com/rustls/rustls) | HTTP, websockets, TLS |
| [serde](https://serde.rs), [toml](https://github.com/toml-rs/toml) | Scenes, assets, project files, packs |
| [notify](https://github.com/notify-rs/notify) | The file watcher behind hot reload |
| [tracing](https://github.com/tokio-rs/tracing) | Logging, into the editor's Output dock |
| [clap](https://github.com/clap-rs/clap) | The `balaur` command line |
| [libloading](https://github.com/nagisa/rust_libloading) | Run-time extensions from shared libraries |

Every crate in the workspace, with its dependencies, is listed in the
generated [Crates](./crates.md) reference.

## glamx, glam and libm

These three look like three choices and are really one. `glamx` is dimforge's
extension crate over `glam` — it adds `Pose2`/`Pose3`/`Rot2` and re-exports
everything else, so depending on it *is* depending on glam. Balaur names only
glamx; `use glamx::Vec3` is the single spelling across the workspace.

`libm` is underneath both. Rapier's `enhanced-determinism` builds glam against
libm instead of the platform's math library, and the workspace pins those
features itself so they hold in every build rather than only when physics
happens to be in the dependency graph. The engine calls libm directly for the
transcendentals that do not come out of a glam type — skeleton rest poses,
slerp, IK, easing curves, and the `math` module scripts call. Same library,
reached two ways. See [Determinism](./manual/determinism.mdx).
