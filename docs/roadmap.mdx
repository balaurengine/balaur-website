---
title: "Roadmap — what the engine does not do yet"
sidebar_label: "Roadmap"
description: "What the Balaur game engine does not do yet and will: editor tools, scripts and assets, networking, platforms and more, each item linked to its plan."
image: "/img/social/roadmap.png"
---

import Roadmap from '@site/src/components/Roadmap';

# Roadmap

What does not exist yet and will. Everything in [Features](/features) is
already in the engine, and the [changelog](./changelog.md) is what each
release added. Within each group the order is the one the
[principles](./principles.mdx) rank it.

## Editor

<Roadmap items={[
  {
    title: 'Tile maps',
    text: <>Collision from the tileset, terrains and autotile, animated and occluding tiles, isometric and hexagonal layouts, a Tiles tool that paints in the viewport, and <code>balaur import</code> for Tiled and LDtk projects.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-tilemap.md',
  },
  {
    title: 'Script completion and hover',
    text: <>Completion, docs on hover, go-to-definition, formatting and rename in <code>balaur lsp</code> and the Script persona, and a VS Code extension over the same server. Diagnostics are all the server publishes today.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-script-tooling.md',
  },
  {
    title: 'Curve editor and onion skin',
    text: 'Tangent handles on keys and ghosted neighbouring frames in the timeline.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-editor.md#curve-editor-and-onion-skin',
  },
  {
    title: 'Rigging panels',
    text: 'A weight table with auto and smooth weights, handles for IK targets, bone names in the viewport, mirror and symmetry, a mesh traced from an image, deform keys, and a bone map for retargeting. The Rig and Polygon tools, four mesh modes with a brush, and the rest-pose verbs are there today.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-editor.md#rigging-panels',
  },
  {
    title: 'Selection, alignment and a library',
    text: <>Select several nodes, box-select, group, align and distribute, hide, lock and isolate; filter the outliner; drop an image, a <code>.glb</code> or a sky onto the viewport and get a node; gizmos for lights and cameras; wireframe and normals views; a pen tool; a material panel; and a library of materials, skies, models and templates to start from.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-editor-ergonomics.md',
  },
  {
    title: 'Network dock and Play as two',
    text: 'Round trip, loss, rollbacks per second and the desync tick per link, and a second instance of the project launched headless and joined over loopback with the fault settings on.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-sessions.md#3-steps',
  },
  {
    title: 'The editor in a browser',
    text: 'The same editor, on a canvas in a tab, with nothing to install. Shipped: the canvas, play and hot reload as on the desktop, a project your browser keeps across a refresh, a folder opened from your own machine and taken back out as a zip, and the two builds a tab can finish by itself — a pack and a web bundle. Left: a project kept on a server rather than in one browser, and the native builds, which need a linker no browser has.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-web-editor.md',
  },
]} />

## Scripts and assets

<Roadmap items={[
  {
    title: 'Texture import settings',
    text: <>Nearest or linear filtering, repeat, mipmaps, anisotropy and sRGB per image, with project defaults and a settings file the editor writes beside the picture; GPU compression at export. A pixel-art sprite cannot ask for hard edges today.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-textures.md',
  },
  {
    title: 'Asset streaming',
    text: 'A load that runs off the tick, a scene added to one already running, and an asset dropped when nothing names it — instead of a pack held whole in memory.',
  },
  {
    title: 'Extensions, tier two',
    text: 'Native extensions that add components and systems, and call back into scripts.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-c-api.md#what-tier-1-does-not-do',
  },
]} />

## Physics and animation

<Roadmap items={[
  {
    title: 'Soft bodies, tearing, fluids',
    text: 'Deformable bodies, cloth that tears, liquids and granular materials, built in Rapier and exposed as components.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-physics.md',
  },
  {
    title: 'Animation blending',
    text: 'Blend trees and state machines that mix clips by weight, and IK in 3D.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-animation-and-resources.md',
  },
  {
    title: 'More of a rig',
    text: <>FABRIK and CCDIK chains, jiggle, a clip played on another rig through a bone map, vertex deform tracks, and a ragdoll built from the bones. <code>look_at</code> and two-bone IK are built.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-animation-and-resources.md#rig-tooling',
  },
  {
    title: 'A sequencer',
    text: 'Cutscenes and cameras on a timeline, with tracks that call something rather than only move it.',
  },
  {
    title: 'Pause, time scale and smooth frames',
    text: <>A pause that holds the game and keeps the menu alive, a <code>process</code> mode per subtree, slow motion and fast forward, motion interpolated between fixed steps so a 144 Hz display sees every frame move, and a tick rate setting.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-time.md',
  },
]} />

## Gameplay and audio

<Roadmap items={[
  {
    title: 'Interactivity without a script',
    text: <>Hover, click, key and resize hooks on any drawn node, named states a node tweens between, scene variables, and event bindings edited in the Events view — each one a call a script could make, and convertible to one. Orbit, first-person, third-person and click-to-move rigs as presets.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-interactivity.md',
  },
  {
    title: 'Navigation',
    text: <>A <code>navmesh</code> asset baked from the scene's colliders and tile maps, paths over it through <code>polyanya</code> or over a grid, and <code>agent2d</code> and <code>agent3d</code> that avoid each other and moving obstacles, all on the fixed step and in the digest so a lockstep game runs it on every peer.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-navigation.md',
  },
  {
    title: 'Voice in a session',
    text: 'Capture, Opus, a jitter buffer, push-to-talk or voice activity, echo cancellation, and each player heard from where their node stands, on a bus like every other sound. Voice never enters the simulation or the digest.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-voice.md',
  },
  {
    title: 'Motion and haptics beyond one pad',
    text: 'Gyro on Switch Pro and Joy-Con, sensor calibration per unit, adaptive triggers and light bars, waveform haptics, motion from a phone or tablet, and gamepads on iOS and Android. A PlayStation pad reports motion and runs both motors today; nothing else does.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-input.md',
  },
]} />

## Rendering

<Roadmap items={[
  {
    title: 'The 3D look',
    text: <>A <code>light3d</code> component with shadows; an <code>environment</code> with sky, image-based lighting, fog, exposure, tonemap and grading; a PBR material with texture maps that needs no shader written and is imported from glTF; glass, mirrors and reflection probes; vignette, grain and the other finishing passes as post-process materials. The only 3D light today is hard-coded.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-3d-rendering.md',
  },
  {
    title: 'Primitives, paths, booleans and cloners',
    text: <>A torus, a pyramid, a prism, a tube and a rounded cuboid with their segment and radius knobs; bezier paths stroked, extruded, lathed and swept; a <code>boolean3d</code> node over its children; a <code>cloner</code> that draws its copies as one call; vertex colours and morph targets from glTF; an ellipse, a star and an ngon in 2D. Every one a mesh built headless, so colliders and picking agree with the screen.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-objects.md',
  },
  {
    title: 'Lit normal-mapped sprites',
    text: <>2D lights and shadows are built. A normal map on <code>sprite</code> needs the light map to carry a direction as well as a colour. Occluding tiles moved to the tile-map plan.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-rendering.md',
  },
  {
    title: 'Text in the world',
    text: <>A name over a character and a score on a sign: <code>text2d</code> and <code>text3d</code> shaped by the same engine the widgets use, outline and shadow, bitmap fonts, and an immediate <code>draw_text_2d</code> for tools.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-text.md',
  },
  {
    title: 'Particles in 3D',
    text: <>A <code>particles3d</code> emitter, and in both dimensions: emission from a sphere, box, cone, ring or mesh, randomness, animated sheets, attractors, colliders, trails and sub-emitters, with a compute stepper where the GPU has one.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-particles.md',
  },
  {
    title: 'Culling and level of detail',
    text: 'Nodes outside the camera skipped and a script that can ask whether one is in view, visibility layers, repeated meshes drawn in one call, and meshes that carry cheaper versions of themselves for distance.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-views-and-culling.md',
  },
  {
    title: 'Drawing terrain',
    text: <>A mesher for the <code>heightfield</code> and <code>voxels</code> assets, and tools to paint them. Physics already collides with both; nothing draws either.</>,
  },
  {
    title: 'More than one view',
    text: <>A <code>viewport</code> component: split screen, a camera rendered to a texture any sprite or material can read, picture-in-picture, and a camera with a projection, field of view and clip planes an author sets.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-views-and-culling.md',
  },
  {
    title: 'Video playback',
    text: 'A movie on a texture with its audio on a bus. Render-side only — a video never feeds simulation state.',
  },
  {
    title: 'Post-process materials',
    text: 'A shader of your own on the camera chain, beside the bloom, SSAO and depth of field already there.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-shaders.md#post-process-materials',
  },
]} />

## Networking

<Roadmap items={[
  {
    title: 'WebTransport in the browser',
    text: 'QUIC runs natively today: reliable streams and unreliable datagrams in one encrypted connection, with datagrams doing the job of raw UDP so the engine never exposes a UDP socket. The browser reaches WebTransport through its own API, which the web build still has to wrap.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#2-transports',
  },
  {
    title: 'Sessions from a script',
    text: 'Host, join and leave from Rune, with a roster whose slots are bound to links; peer, host and headless server roles, the server ordering inputs, verifying every client’s digest and holding the history a late joiner replays from. Rollback runs today between two engines in a Rust test only.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-sessions.md',
  },
  {
    title: 'Late join, reconnect and host migration',
    text: 'A snapshot on its own stream for whoever arrives late, a grace period that keeps a dropped player’s slot, spectators, the next member taking over when the host vanishes, and bincode instead of JSON on the wire.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-sessions.md#3-steps',
  },
  {
    title: 'State replication and RPC',
    text: <>A <code>replicate</code> key on a node — authority, components, mode — and the server sends deltas off the component schema; RPCs address nodes by stable id.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#3-steps',
  },
  {
    title: 'Client prediction and reconciliation',
    text: 'Your own node moves the frame you press the key, and the server’s correction rewinds that node alone and replays what is still pending, smoothed out over a few frames. Everyone else is drawn a little behind and interpolated.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#hiding-latency',
  },
  {
    title: 'Lag compensation and interest management',
    text: 'The server rewinds to the tick the shooter saw before testing a hit, and sends each client only what it can see, at a send rate below the tick rate.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#3-steps',
  },
  {
    title: 'WebRTC data channels',
    text: 'Browser peer-to-peer without a relay, behind the same transport trait. Built only once a game needs it; on native, Steam’s Datagram Relay answers the same NAT in the Steam plan.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#2-transports',
  },
]} />

## Gamend

Server work lives in the [Gamend repository](https://github.com/appsinacup/gamend); the plan is kept beside the engine's so the two sides agree.

<Roadmap items={[
  {
    title: 'A game server per lobby',
    text: 'Gamend launches a headless Balaur when a lobby starts, records its address and certificate hash on the lobby, and players connect to it over QUIC. Gamend keeps the roster; the game keeps the ticks.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#3-steps',
  },
  {
    title: 'Lobby tokens and rejoin',
    text: 'A lobby-scoped token binds a player to their slot on the game server and lets them back in after a dropped socket. Leaderboard writes come from the server that saw the match, never from the client that claims a score.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#1-design',
  },
  {
    title: 'The match record',
    text: 'The server’s recording and final digest uploaded as the lobby’s record, so a disputed result is a replay that either matches or names the tick.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#1-design',
  },
  {
    title: 'A WebRTC relay for browsers',
    text: 'The server-side WebRTC peer generalised from hook calls to unordered, unreliable data channels per lobby, so two browsers play through Gamend before the engine has WebTransport in a tab.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#2-the-surface',
  },
  {
    title: 'Typed bindings for the whole API',
    text: <>One script call per Gamend operation, generated from its OpenAPI document and realtime protobuf, with typed events per channel; <code>rest</code> and <code>push</code> stay for the hooks a game adds.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#engine-side-in-this-repository',
  },
  {
    title: 'Skill matchmaking',
    text: 'Ratings, widening bands and leaver refill, on Gamend’s own roadmap; the engine binds whatever the API exposes.',
    plan: 'https://github.com/appsinacup/gamend/blob/main/ROADMAP.md',
  },
]} />

## Tools and services

<Roadmap items={[
  {
    title: 'An MCP server',
    text: <><code>balaur mcp</code>: the project, its components and API, <code>check</code>, a headless run and a screenshot as tools an agent drives from Claude Code, Cursor or an IDE, and the running editor's palette behind a second flag. Files are the API, so the editor follows every edit.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-mcp.md',
  },
  {
    title: 'Projects in the cloud',
    text: <>A project on a Gamend account with a version per save, a share link with a role, who else is in the scene and what they have selected, comments pinned to nodes, and editing together — under a lock per scene first, over a CRDT if a team asks.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-collaboration.md',
  },
  {
    title: 'Accessibility',
    text: 'A screen reader over the widget tree, text scaling, captions and colour-blind-safe defaults. Focus order, localization and rebindable actions are already there.',
  },
  {
    title: 'A crash report that reproduces itself',
    text: 'The recording, the log and the build id in one file, so a bug report re-runs the session that caused it bit for bit.',
  },
  {
    title: 'Steam',
    text: <>Sign-in, achievements, leaderboards, Cloud saves, rich presence, the overlay, Workshop, Steam Input and Steam networking — behind the <code>platform</code> module Apple's stores already speak, so one script runs everywhere.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-steam.md',
  },
  {
    title: 'Google Play',
    text: 'Play Games Services, Sign in with Google, Play Billing, Play Integrity and Play Asset Delivery on Android, over the same module. An application id, an app bundle and 16 KB page alignment come first: without them Play refuses the upload, store or no store.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-google.md',
  },
]} />

## Shipping

<Roadmap items={[
  {
    title: 'Signed releases',
    text: 'The nightly and tagged builds, their runtime templates and the updater exist. What is missing is a download per platform that is signed and notarized, so it opens without a warning.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-release.md#binary-releases',
  },
  {
    title: 'One-click deploy',
    text: 'Deploy to the web or to a phone from one command or one button: the game built, signed and put somewhere a player can reach it — a URL, itch.io, a device on the cable, TestFlight, Play or Steam. Today an export leaves a bundle on your disk and stops.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-deploy.md',
  },
  {
    title: 'Embedding on a page',
    text: <>A <code>&lt;balaur-viewer&gt;</code> element and an npm runtime with a React wrapper, a page API to set variables and hear events over the bridge that already exists, a web module sized to what the game uses instead of one that carries everything, a progressive web app option on the export, and image, video and glTF export from the editor.</>,
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-embed.md',
  },
  {
    title: 'Console export',
    text: 'Switch, PlayStation and Xbox. The export and the pack shape travel; each console’s graphics, input and store layer is an NDA SDK that is none of the crates the engine runs on.',
  },
  {
    title: 'XR',
    text: 'OpenXR on desktop and standalone headsets, WebXR in the browser: stereo views, tracked poses, controller and hand input.',
  },
  {
    title: 'Published benchmarks',
    text: 'What a frame costs, measured headless and posted with every release.',
    plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-release.md#published-benchmarks',
  },
]} />

[Discord](https://discord.gg/v649emcpAu) ·
[Issues](https://github.com/balaurengine/balaur/issues) ·
[Discussions](https://github.com/balaurengine/balaur/discussions)
