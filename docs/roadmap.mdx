---
title: "Roadmap — what is built, and what each milestone adds"
sidebar_label: "Roadmap"
description: "What the Balaur game engine does today and what each milestone adds next: the editor, rendering, physics, networking, platforms and shipping, one tab per version."
image: "/img/social/roadmap.png"
hide_table_of_contents: true
---

import Roadmap from '@site/src/components/Roadmap';

# Roadmap

<Roadmap milestones={[
  {
    id: '0.1',
    state: 'built',
    estimate: "September 2026",
    title: "The engine as it stands",
    items: [
      {
        group: "Editor",
        title: 'The editor',
        text: <>A stage shell of tabbed, resizable docks built from the engine's own widgets, with undo, prefabs, ray picking and the Rig, Polygon and Tiles tools.</>,
        image: "/img/manual/editor_overview.webp",
        alt: "The editor on examples/angrynerds: the node tree, the viewport with its 2D grid and gizmo, the inspector, and the dock tabs",
        posts: [
          {slug: "profiler", title: "Profiler dock"},
        ],
      },
      {
        group: "Editor",
        title: 'Tile maps',
        text: <>A tileset that says what each tile is, with voxel collision, autotiling, animated tiles, isometric and hexagonal layouts, and Tiled and LDtk import.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-tilemap.md',
        image: "/img/manual/tiles_overview.webp",
        alt: "The Tiles dock painting a ground layer, with tile_collision on the map in the inspector",
        posts: [
          {slug: "tile-maps", title: "Tile maps"},
        ],
      },
      {
        group: "Editor",
        title: 'Script completion and hover',
        text: <>Completion, hover, go-to-definition, references, rename and formatting, in the Script persona, in <code>balaur lsp</code> and in a VS Code extension.</>,
        image: "/img/manual/script_completion.webp",
        alt: "The completion popup: a module path, and the functions and constants under it",
        posts: [
          {slug: "what-completes-at-the-caret", title: "Completion, hover and go to definition"},
          {slug: "language-server", title: "Linting and a language server"},
        ],
      },
      {
        group: "Editor",
        title: 'Selection, alignment and a library',
        text: <>Multi-select, align and distribute, gizmos, a Pen tool, a material panel, an Events view, a Cost dock, and <code>editor/library</code>.</>,
        image: "/img/manual/editor_selection.webp",
        alt: "The Library dock open under the viewport, with a torus selected and pointed at the library's metal material",
        posts: [
          {slug: "select-two-things", title: "Multi-select, lights and PBR materials"},
        ],
      },
      {
        group: "Scripts and assets",
        title: 'Rune scripting',
        text: <>One deterministic language with a <code>math</code> module, component handles on nodes, hot reload, a debugger over DAP, and a self-documenting API.</>,
        image: "/img/manual/scripting_editor.webp",
        alt: "The Script persona: the file, the hooks it declares, and the events its node receives",
        posts: [
          {slug: "rune-scripting", title: "Rune scripting"},
          {slug: "generated-script-reference", title: "Generated script reference"},
        ],
      },
      {
        group: "Scripts and assets",
        title: 'Scenes, assets and packs',
        text: <>Prefabs with per-path overrides, <code>id://</code> references that survive a rename, import settings beside each file, and sha256-verified binary packs.</>,
        image: "/img/manual/editor_assets.webp",
        alt: "The Assets dock on res://, with the project file, the scenes and the scripts beside the tree",
        posts: [
          {slug: "asset-hot-reload", title: "Asset hot reload and renaming"},
          {slug: "settings-beside-the-file", title: "Asset import settings"},
        ],
      },
      {
        group: "Physics and animation",
        title: 'Rapier in 2D and 3D',
        text: <>Bodies, joints, character controllers, the query pipeline, collision events, ray-cast vehicles, and every collider shape including editable voxels.</>,
        image: "/img/manual/physics_collapse.webp",
        alt: "The Physics persona's collider overlays over the towers of examples/angrynerds",
        posts: [
          {slug: "2d-support", title: "2D support"},
        ],
      },
      {
        group: "Physics and animation",
        title: 'Rigs and animation',
        text: <>2D and 3D skeletons with five modifiers each, GPU skinning, deform and morph tracks, retargeting through a <code>bone_map</code>, ragdolls and tweens.</>,
        image: "/img/manual/rigging_modifiers.webp",
        alt: "The Rig tool drawing a modifier's chain, the dashed line to its target and the handle that drags it",
        posts: [
          {slug: "skeletons-and-a-debugger", title: "Skeletons, a debugger, and a fixed step"},
          {slug: "gpu-skinning", title: "GPU skinning"},
          {slug: "rigging-panels", title: "IK, retargeting and ragdolls"},
        ],
      },
      {
        group: "Gameplay and audio",
        title: 'Interactivity without a script',
        text: <>Hooks, states, scene variables and the binding rows the Events view authors over them, so a scene reacts with no <code>.rn</code> beside it.</>,
        image: "/img/manual/editor_events.webp",
        alt: "The Events view: two binding rows on the ball, one counting the click and one opening the door when the score reaches three",
        posts: [
          {slug: "select-two-things", title: "Multi-select, lights and PBR materials"},
        ],
      },
      {
        group: "User interface",
        title: 'Widgets, text and the batteries',
        text: <>Nineteen widget kinds over cosmic-text, containers that hand out rects, text in the world, and the batteries: audio buses, input actions, saves, localisation.</>,
        image: "/img/manual/ui_widgets.webp",
        alt: "The Interface persona: a HUD's widgets in the tree, drawn over the safe area, and a button in the inspector",
        posts: [
          {slug: "text-in-the-world", title: "Text2D and Text3D"},
          {slug: "audio-buses", title: "Audio buses and positional audio"},
          {slug: "input-actions-and-gamepads", title: "Input actions and gamepads"},
          {slug: "save-games", title: "Save games"},
          {slug: "localization", title: "Localization"},
        ],
      },
      {
        group: "Rendering",
        title: 'What draws today',
        text: <>Sprites, tile maps, 2D lights, <code>light3d</code> and an <code>environment</code>, PBR, WESL shaders, the finishing passes, sixteen primitives, booleans and particles.</>,
        image: "/img/manual/rendering_directional.webp",
        alt: "Three occluders under one directional light, each casting a parallel shadow strip at the light's angle",
        posts: [
          {slug: "sprites", title: "Sprites"},
          {slug: "2d-lights-and-shadows", title: "2D lights and shadows"},
          {slug: "every-shape-is-a-mesh", title: "Primitive shapes, paths and booleans"},
        ],
      },
      {
        group: "Rendering",
        title: 'Post-process materials',
        text: <>A name in <code>camera.post</code> the engine does not know is a <code>material</code> drawn over the whole frame, with <code>tonemap</code> the boundary.</>,
        image: "/img/manual/shader_preview.webp",
        alt: "A material in the inspector, its shader open in the code editor, and the UV channel drawn instead of the picture",
        posts: [
          {slug: "shaders-and-materials", title: "Shaders and materials"},
        ],
      },
      {
        group: "Networking",
        title: 'The deterministic core',
        text: <>A fixed 60 Hz step, a per-tick digest checked across operating systems, record and replay, rollback, and three transports behind one trait.</>,
        image: "/img/manual/determinism_replay.webp",
        alt: "The Session dock playing a recorded run back, with nobody at the mouse",
        posts: [
          {slug: "rollback-networking", title: "Rollback networking"},
        ],
      },
      {
        group: "Tools and services",
        title: 'Apple and the platform module',
        text: <>Sign-in, achievements, leaderboards, cloud saves and purchases behind one module, over Game Center, iCloud and StoreKit, with an export that signs.</>,
        image: "/img/manual/export_sheet.webp",
        alt: "The Export sheet: the ios, android, macos-universal, windows and web targets, each saying what it still needs",
        posts: [
          {slug: "game-center-icloud-and-purchases", title: "Game Center, iCloud and in-app purchases"},
          {slug: "mobile-export", title: "Mobile export and signing"},
        ],
      },
      {
        group: "Shipping",
        title: 'Export, the web and the CLI',
        text: <><code>balaur export</code> for native and web with a size report, <code>balaur test</code>, a browser editor over IndexedDB, and a benchmark suite.</>,
        image: "/img/manual/export_size.webp",
        alt: "Where a pack's bytes are, and what lossless re-encoding takes off",
        posts: [
          {slug: "what-an-export-weighs", title: "Smaller exports"},
          {slug: "editor-in-the-browser", title: "The editor in the browser"},
        ],
      },
    ],
  },
  {
    id: '0.2',
    state: 'building',
    estimate: "December 2026",
    title: "Nothing blocks an ordinary game",
    items: [
      {
        group: "Editor",
        title: 'Focused script editing',
        text: <>The code pane takes the whole window, with the docks and the hooks list folded away and put back exactly as they were.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-editor-redesign.md#57-focus-for-a-script',
      },
      {
        group: "Scripts and assets",
        title: 'A bad call caught before it runs',
        text: <><code>balaur check</code> reads the scene beside the script and resolves a component's method against the table the run time uses.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-script-analysis.md',
      },
      {
        group: "Scripts and assets",
        title: 'Texture import settings',
        text: <>Repeat, mipmaps, anisotropy and premultiplied alpha, which wait on a sampler the renderer does not expose, plus GPU compression at export.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-textures.md',
      },
      {
        group: "Physics and animation",
        title: 'Animation blending',
        text: <>Blend trees and state machines, so a rig that is otherwise complete can run two clips at once.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-animation-and-resources.md',
      },
      {
        group: "Physics and animation",
        title: 'Pause, time scale and smooth frames',
        text: <>A <code>process</code> mode per subtree, time scale, interpolation between fixed steps, <code>max_fps</code>, vsync, and a tick rate setting.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-time.md',
      },
      {
        group: "User interface",
        title: 'Lists, trees and tables',
        text: <><code>list</code>, <code>tree</code> and <code>table</code> kinds with selection, columns, and only the rows in view built, which every dock hand-rolls today.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-widgets.md#data-views',
      },
      {
        group: "User interface",
        title: 'Menus, popups and tooltips',
        text: <>A menu bar, a context menu, a tooltip and a toast in a scene, over a pass that draws above the widget tree.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-widgets.md#menus-and-popups',
      },
      {
        group: "User interface",
        title: 'Text a game can edit',
        text: <>A multi-line <code>text</code> kind, a <code>code</code> kind with the gutter and colouring the editor has, a numeric <code>spin</code>, and links a label reports.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-widgets.md#text',
      },
      {
        group: "Rendering",
        title: 'The 3D look',
        text: <>Image-based lighting and SSAO bound, glTF keeping its factors and maps, glass, mirrors, probes, finishing passes and layer stacks.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-3d-rendering.md',
      },
      {
        group: "Networking",
        title: 'Sessions from a script',
        text: <>Host, join and leave from Rune, with a roster bound to links and <code>peer</code>, <code>host</code> and headless <code>server</code> roles.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-sessions.md',
      },
      {
        group: "Shipping",
        title: 'Signed releases',
        text: <>Signed binaries per platform, cut by the release workflow.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-release.md#binary-releases',
      },
    ],
  },
  {
    id: '0.3',
    state: 'planned',
    estimate: "March 2027",
    title: "Everything you see",
    items: [
      {
        group: "Editor",
        title: 'Semantic tokens, inlay hints and code actions',
        text: <>Colouring from the compiler, inferred types shown inline, and a quick fix on a diagnostic, held until the gaps in completion are known.</>,
      },
      {
        group: "Editor",
        title: 'Curve editor and onion skin',
        text: <>Tangent handles on keys, and ghosted neighbouring frames in the timeline.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-editor.md#curve-editor-and-onion-skin',
      },
      {
        group: "Physics and animation",
        title: 'A sequencer',
        text: <>Cutscenes and cameras on a timeline, with tracks that call something rather than only move it.</>,
      },
      {
        group: "User interface",
        title: 'Aspect and camera containers',
        text: <>An <code>aspect</code> box that holds a ratio, and a camera's texture drawn as a widget the layout sizes.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-widgets.md#containers',
      },
      {
        group: "Rendering",
        title: 'Lit normal-mapped sprites',
        text: <>2D lights and shadows are built, and the normal map is what is left.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-rendering.md',
      },
      {
        group: "Rendering",
        title: 'Particles in 3D',
        text: <><code>particles3d</code>, and in both dimensions: emission shapes, attractors, colliders, trails, sub-emitters, lit particles and a compute stepper.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-particles.md',
      },
      {
        group: "Rendering",
        title: 'Culling and level of detail',
        text: <>Frustum culling and <code>render.in_view</code>, cull masks, automatic instancing, MSAA, level of detail in the mesh asset, 2D batching and <code>multimesh</code>.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-views-and-culling.md',
      },
      {
        group: "Rendering",
        title: 'Voxels and terrain',
        text: <>Block types in a <code>voxel_set</code>, a greedy chunk mesher, a chunked grid file, a Voxels tool, <code>.vox</code> import, and heightfield meshing.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-voxels.md',
      },
      {
        group: "Rendering",
        title: 'More than one view',
        text: <>A <code>viewport</code> component for split screen, a camera rendered to a texture, and picture-in-picture.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-views-and-culling.md',
      },
      {
        group: "Rendering",
        title: 'Video playback',
        text: <>A movie on a texture with its audio on a bus, render-side only.</>,
      },
    ],
  },
  {
    id: '0.4',
    state: 'planned',
    estimate: "July 2027",
    title: "Multiplayer, end to end",
    items: [
      {
        group: "Editor",
        title: 'Network dock and Play as two',
        text: <>Two peers started from the editor, with the link and its stats in a dock.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-sessions.md#3-steps',
      },
      {
        group: "Networking",
        title: 'WebTransport in the browser',
        text: <>Native QUIC datagrams, binary frames, stable ids and rollback are built, and the browser side is not.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#2-transports',
      },
      {
        group: "Networking",
        title: 'Late join, reconnect and host migration',
        text: <>Under lockstep, out of the snapshot ring.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-sessions.md#3-steps',
      },
      {
        group: "Networking",
        title: 'State replication and RPC',
        text: <>Deltas generated off the component registry, addressed by <code>StableId</code> rather than by path.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#3-steps',
      },
      {
        group: "Networking",
        title: 'Client prediction and reconciliation',
        text: <>The client runs ahead and is corrected against the server, with nodes it does not own interpolated a send interval behind.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#hiding-latency',
      },
      {
        group: "Networking",
        title: 'Lag compensation and interest management',
        text: <>A server rewinding to the tick the shooter saw, and a bandwidth budget per observer.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#3-steps',
      },
      {
        group: "Gamend",
        title: 'A game server per lobby',
        text: <>Launched and registered by Gamend.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#3-steps',
      },
      {
        group: "Gamend",
        title: 'Lobby tokens and rejoin',
        text: <>Lobby-scoped tokens, and a grace period to rejoin.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#1-design',
      },
      {
        group: "Gamend",
        title: 'The match record',
        text: <>The match recording uploaded as the lobby's record.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#1-design',
      },
      {
        group: "Gamend",
        title: 'Typed bindings for the whole API',
        text: <>In place of <code>rest</code> and <code>push</code>, over the nine calls the <code>gamend</code> module has today.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#engine-side-in-this-repository',
      },
    ],
  },
  {
    id: '0.5',
    state: 'planned',
    estimate: "November 2027",
    title: "A game built without writing a script",
    items: [
      {
        group: "Editor",
        title: 'A graph that writes Rune',
        text: <>A <code>graphs/*.toml</code> asset whose save emits the <code>.rn</code> beside it, over a node palette generated from <code>api.json</code>.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-authoring-without-code.md',
      },
      {
        group: "Scripts and assets",
        title: 'Scripts you can take',
        text: <>Fifteen small scripts with <code>exports()</code> in the library dock, dropped onto a node and edited in place.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-authoring-without-code.md',
      },
      {
        group: "Scripts and assets",
        title: 'Extensions, tier two',
        text: <>Components, systems and calling back into scripts, across the C boundary.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-c-api.md#what-tier-1-does-not-do',
      },
      {
        group: "Gameplay and audio",
        title: 'Navigation',
        text: <>A <code>navmesh</code> asset, paths over it, and <code>agent2d</code> and <code>agent3d</code> with avoidance, all on the fixed step and in the digest.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-navigation.md',
      },
      {
        group: "Gameplay and audio",
        title: 'Voice in a session',
        text: <>Capture, Opus, a jitter buffer, push-to-talk, echo cancellation and positional voice, never entering the simulation or the digest.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-voice.md',
      },
      {
        group: "User interface",
        title: 'A graph canvas',
        text: <>A <code>graph</code> kind of nodes, ports and links a drag connects, with pan and zoom, under both the Rune graph and the shader graph.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-widgets.md#containers',
      },
      {
        group: "User interface",
        title: 'Pickers, and drag and drop',
        text: <>A colour wheel and a file chooser as widget kinds, with a payload one widget offers and another takes.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-widgets.md#pickers-and-drag',
      },
      {
        group: "Tools and services",
        title: 'An MCP server',
        text: <><code>balaur mcp</code> over stdio, with the project, <code>check</code>, a headless run and a screenshot as tools an agent drives.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-mcp.md',
      },
      {
        group: "Tools and services",
        title: 'A crash report that reproduces itself',
        text: <>The recording, the log and the build id in one file.</>,
      },
    ],
  },
  {
    id: '0.6',
    state: 'planned',
    estimate: "April 2028",
    title: "A game gets out",
    items: [
      {
        group: "Editor",
        title: 'The editor in a browser',
        text: <>A project kept on Gamend rather than only in the browser, and native targets exported from a tab.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-web-editor.md',
      },
      {
        group: "Scripts and assets",
        title: 'Asset streaming',
        text: <>A load that runs off the tick, a scene added to one already running, and an asset dropped when nothing names it.</>,
      },
      {
        group: "User interface",
        title: 'Accessibility',
        text: <>A screen reader over the widget tree, text scaling, captions, and colour-blind-safe defaults.</>,
      },
      {
        group: "Rendering",
        title: 'WebGL2 beside WebGPU',
        text: <>A browser without WebGPU falls back to a GL backend picked at boot, minus the compute passes it cannot serve.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-web-backends.md',
      },
      {
        group: "Tools and services",
        title: 'Steam',
        text: <>Sign-in, achievements, leaderboards, cloud saves and purchases behind <code>platform</code>, with the overlay, Workshop, lobbies and Steam Input beside it.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-steam.md',
      },
      {
        group: "Tools and services",
        title: 'Google Play',
        text: <>The same set, on Play Games Services.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-google.md',
      },
      {
        group: "Shipping",
        title: 'One-click deploy',
        text: <>A game on a URL or on a phone from one command or one button.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-deploy.md',
      },
      {
        group: "Shipping",
        title: 'A web module that loads in parts',
        text: <>The wasm split so a game fetches the physics, audio or networking it uses, with a pack arriving in pieces beside it.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-embed.md',
      },
      {
        group: "Shipping",
        title: 'Embedding on a page',
        text: <>An npm runtime with a <code>&lt;balaur-viewer&gt;</code> element and a React wrapper, a typed page API, and a web module sized to the game.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-embed.md',
      },
      {
        group: "Shipping",
        title: 'Sealed packs and stripped binaries',
        text: <>Bytecode on the web too, and a pack sealed with ChaCha20-Poly1305 under a project key, never a DRM wrapper.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-protection.md',
      },
    ],
  },
  {
    id: '0.7',
    state: 'planned',
    estimate: "September 2028",
    title: "What a bigger game asks for",
    items: [
      {
        group: "Scripts and assets",
        title: 'FBX import',
        text: <><code>balaur import</code> over the <code>ufbx</code> crate, for the meshes, rigs and clips that never ship as glTF.</>,
      },
      {
        group: "Scripts and assets",
        title: 'Faster transcendentals',
        text: <><code>sin</code>, <code>cos</code>, <code>exp</code>, <code>log</code> and <code>pow</code> on polynomial approximations rather than <code>libm</code>, deterministic either way, chosen per project.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-fast-math.md',
      },
      {
        group: "Scripts and assets",
        title: 'Rune compiled to WebAssembly',
        text: <>A script unit built to a <code>.wasm</code> module ahead of time and run over the same host calls, in place of the interpreter.</>,
      },
      {
        group: "Physics and animation",
        title: 'Soft bodies',
        text: <><code>softbody2d</code> and <code>softbody3d</code>: a deformable mesh with stiffness, damping and pressure, drawn down the skinning path, on the fixed step.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-physics.md#soft-bodies',
      },
      {
        group: "Physics and animation",
        title: 'Cloth and rope',
        text: <>A sheet that hangs and a rope of linked segments over the same solver, pinned to a node and cut by a script.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-physics.md#cloth-and-rope',
      },
      {
        group: "Physics and animation",
        title: 'Root motion',
        text: <>A clip that moves the character rather than sliding under it, its root delta handed to <code>character2d</code> and <code>character3d</code> per tick.</>,
      },
      {
        group: "Networking",
        title: 'WebRTC data channels',
        text: <>For browser peer-to-peer without a relay, never over raw UDP and never over ENet.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#2-transports',
      },
      {
        group: "Gamend",
        title: 'A WebRTC relay for browsers',
        text: <>So a browser peer joins a session through Gamend.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#2-the-surface',
      },
      {
        group: "Gamend",
        title: 'Skill matchmaking',
        text: <>Queues and ratings, whose work is in the Gamend server.</>,
        plan: 'https://github.com/appsinacup/gamend/blob/main/ROADMAP.md',
      },
      {
        group: "Tools and services",
        title: 'Projects in the cloud',
        text: <>Files on a Gamend account with a version per save, share links with roles, presence in the viewport, and comments anchored to nodes.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-collaboration.md',
      },
      {
        group: "Shipping",
        title: 'Per-platform project settings',
        text: <><code>[application.android]</code> and its siblings over <code>project.toml</code>: window mode, tick rate, feature set and asset variant per target.</>,
      },
    ],
  },
  {
    id: '0.8',
    state: 'planned',
    estimate: "March 2029",
    title: "The machine a game runs on",
    items: [
      {
        group: "Gameplay and audio",
        title: 'Motion and haptics beyond one pad',
        text: <>Switch Pro and Joy-Con gyro, sensor calibration, adaptive triggers, waveform haptics, device motion, and pads on iOS and Android.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-input.md',
      },
      {
        group: "Gameplay and audio",
        title: 'Touch controls a phone needs',
        text: <><code>touch_button</code> and <code>touch_stick</code> widget kinds, pinch, swipe and long-press recognisers, and a keyboard height a layout can read.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-input.md',
      },
      {
        group: "User interface",
        title: 'A controller-only shell',
        text: <>Directional focus between widgets, an on-screen keyboard, safe-area insets applied to layout, and button glyphs that follow the pad.</>,
      },
      {
        group: "Shipping",
        title: 'The shell a phone has',
        text: <>Opening a link works on every desktop and in a browser tab, and on neither phone, which each want a call of their own.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-mobile-export.md',
      },
      {
        group: "Shipping",
        title: 'What a phone lends a game',
        text: <>The share sheet, the camera, geolocation, biometrics, the clipboard, keep-awake and vibration, behind one <code>device</code> module.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-2d-games.md',
      },
      {
        group: "Shipping",
        title: 'A game on a small machine',
        text: <><code>linux-arm64</code> is an export target that has never run, so a Raspberry Pi wants the GL backend and a measured frame budget.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-release.md',
      },
      {
        group: "Shipping",
        title: 'Suspend and resume',
        text: <>Hooks a script answers on a phone call or a console's suspend, audio released and taken back, and a tick that pauses.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-mobile-export.md',
      },
      {
        group: "Shipping",
        title: 'Console export',
        text: <>Switch, PlayStation and Xbox, each an NDA SDK that is not wgpu, winit or gilrs.</>,
      },
      {
        group: "Shipping",
        title: 'XR',
        text: <>OpenXR on desktop and standalone headsets, WebXR in the browser, and ARKit and ARCore behind the same seam.</>,
      },
    ],
  },
  {
    id: '0.9',
    state: 'planned',
    estimate: "September 2029",
    title: "A world, not a scene",
    items: [
      {
        group: "Scripts and assets",
        title: 'Procedural noise',
        text: <>A <code>noise</code> module of value, perlin, simplex and worley with fbm over them, seeded like <code>rng</code> and computed on <code>libm</code>.</>,
      },
      {
        group: "Scripts and assets",
        title: 'A world bigger than a float',
        text: <>Origin rebasing on the fixed step, so a world runs past the precision <code>f32</code> has left, with the scene streamed in chunks.</>,
      },
      {
        group: "Physics and animation",
        title: 'Tearing',
        text: <>A threshold on a soft body: past it the body splits into two bodies and two meshes, mid-step and in the digest.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-physics.md#tearing',
      },
      {
        group: "Physics and animation",
        title: 'Falling sand',
        text: <>A 2D cellular grid of sand, water, lava and fire, in a <code>balaur_cells</code> plugin on the fixed step and in the digest.</>,
      },
      {
        group: "Physics and animation",
        title: 'Fluids',
        text: <><code>fluid2d</code> and <code>fluid3d</code>: particles with a rest density and a viscosity, with emitters and drains, drawn as points before a surface.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-physics.md#fluids',
      },
      {
        group: "Gameplay and audio",
        title: 'Sound that fills a room',
        text: <>Effects on a bus: reverb, EQ, a compressor and a limiter, ducking, HRTF, and long music streamed rather than decoded whole.</>,
      },
      {
        group: "Rendering",
        title: 'Terrain sculpting and foliage',
        text: <>Brushes over the <code>heightfield</code> asset, splat maps for the textures between them, and grass and trees scattered by painting.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-voxels.md',
      },
      {
        group: "Rendering",
        title: 'Occlusion culling',
        text: <>What stands behind a wall skipped, over a software depth rasteriser.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-views-and-culling.md',
      },
      {
        group: "Rendering",
        title: 'Global illumination',
        text: <>Light that bounces, in real time: a screen-space pass or an SDF probe field, since nothing in the kiss3d fork does it.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-3d-rendering.md',
      },
      {
        group: "Rendering",
        title: 'Decals and volumetric fog',
        text: <>A texture projected onto whatever is under it, and fog a light shafts through, both passes of our own.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-3d-rendering.md',
      },
    ],
  },
  {
    id: '1.0',
    state: 'planned',
    estimate: "March 2030",
    title: "The editor makes the content",
    items: [
      {
        group: "Editor",
        title: 'More than one window',
        text: <>A second OS window: a dock torn off the editor, or a game on a second display.</>,
      },
      {
        group: "Editor",
        title: 'A shader graph',
        text: <>The canvas the Rune graph brings, emitting WESL instead of Rune, with <code>[params]</code> still read off the linked shader.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-shaders.md',
      },
      {
        group: "Editor",
        title: 'Modelling in the viewport',
        text: <>Push and pull a face, bevel an edge, subdivide and unwrap with <code>xatlas</code>, editing the <code>mesh</code> asset every shape already is.</>,
      },
      {
        group: "Physics and animation",
        title: 'Gases and smoke',
        text: <>A buoyant volume that rises, spreads and cools, read by the renderer as a density field rather than as particles.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-physics.md#gases-and-smoke',
      },
      {
        group: "Physics and animation",
        title: 'Granular materials',
        text: <>Sand, mud and snow as a fluid with friction and a yield stress, in 2D and 3D, on the fixed step.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-physics.md#granular-materials',
      },
      {
        group: "Gameplay and audio",
        title: 'Behaviour trees',
        text: <>A tree asset ticked on the fixed step and in the digest, with the navigation agents and the script API as its leaves.</>,
      },
      {
        group: "Gameplay and audio",
        title: 'Dialogue',
        text: <>A <code>dialogue</code> plugin over an ink-shaped script, its lines addressed by key so <code>strings.tr</code> translates them.</>,
      },
      {
        group: "Gameplay and audio",
        title: 'Translations as a pipeline',
        text: <>Import from <code>.csv</code> and gettext <code>.po</code>, an asset remapped per locale, a dock naming every missing key, and pseudolocalisation.</>,
      },
    ],
  },
  {
    id: '1.1',
    state: 'planned',
    estimate: "July 2030",
    title: "Other people's parts",
    items: [
      {
        group: "Scripts and assets",
        title: 'More importers',
        text: <>Spine and DragonBones for 2D skeletal animation, layered PSD, and <code>.blend</code> read by calling Blender.</>,
      },
      {
        group: "Scripts and assets",
        title: 'A package manager',
        text: <><code>[dependencies]</code> in <code>project.toml</code>, <code>balaur add</code>, and a lockfile carrying a hash per entry, resolved against the catalogue.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-plugins.md',
      },
      {
        group: "Scripts and assets",
        title: 'Extensions in WebAssembly',
        text: <>A third tier beside the Rust and C ones: a <code>.wasm</code> module over <code>wasmtime</code> natively and the browser's own engine on the web.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-plugins.md',
      },
      {
        group: "Tools and services",
        title: 'A library others publish to',
        text: <>A Gamend-hosted catalogue over the manifest <code>editor/library/</code> already uses, with a hash per entry and a name saying who published it.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-collaboration.md',
      },
    ],
  },
]} />

[Discord](https://discord.gg/v649emcpAu) ·
[Issues](https://github.com/balaurengine/balaur/issues) ·
[Discussions](https://github.com/balaurengine/balaur/discussions)
