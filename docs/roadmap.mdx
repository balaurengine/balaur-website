---
title: "Roadmap — what is built, and what each milestone adds"
sidebar_label: "Roadmap"
description: "What the Balaur game engine does today and what each milestone adds next: the editor, rendering, physics, networking, platforms and shipping, one tab per version."
image: "/img/social/roadmap.png"
hide_table_of_contents: true
---

import Roadmap from '@site/src/components/Roadmap';

# Roadmap

<Roadmap milestones={[
  {
    id: '0.1',
    state: 'built',
    title: "The engine as it stands",
    items: [
      {
        group: "Editor",
        title: 'The editor',
        text: <>A stage shell of tabbed, resizable, animated docks, built out of the engine's own widgets and themed with them, so the editor is a Balaur application. Undo and redo, copy and paste, a searchable inspector, prefab instances with overrides, ray picking, a profiler dock, a narrow-window layout, and the Rig, Polygon and Tiles tools. Renaming a file in the Assets dock rewrites every reference to it.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Editor",
        title: 'Tile maps',
        text: <>A tileset says what each tile is, and the map draws it: collision as one voxel shape, autotiling from an ordered rule table with templates, animated and light-blocking tiles, per-tile data, isometric and hexagonal layouts, the brushes and the Set panel, cells a level may keep in its own file, and <code>balaur import</code> for Tiled and LDtk. Quarter-tile sheets draw a cell as four quarters picked by the cells touching each corner, so a five-tile sheet covers all 47 neighbourhoods.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-tilemap.md',
      },
      {
        group: "Editor",
        title: 'Script completion and hover',
        text: <>Type <code>physics2d::</code> and the list is that module's 66 functions and 48 constants, each with the line the reference prints. Type <code>node.body2d.</code> and it is what acts on that component. Hover names a function's signature and what it does; go-to-definition opens the file, or the Docs dock when the definition is the engine's. <code>balaur fmt</code> lays a project out, rename rewrites every file a script's <code>mod</code> declarations reach, and the same server answers a VS Code, Zed or Neovim client.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Editor",
        title: 'Selection, alignment and a library',
        text: <>Built. The selection is a set: shift and ⌘ click, box select in both viewports, and a gizmo drag that moves, turns and scales the whole set. Group, align, distribute, hide, lock, isolate; outliner facet chips; a dropped file becoming a node; light, camera and frustum gizmos; a view-mode chip and camera bookmarks; a Pen tool over <code>path2d</code>; a material panel with texture slots; a Cost dock; and a Library of materials, skies, models, rigs, lighting setups and templates behind <code>balaur new --template</code>.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Scripts and assets",
        title: 'Rune scripting',
        text: <>One language, chosen for being deterministic: Rune, with a <code>math</code> module that answers the same on every platform. Exported properties, component handles on nodes, hot reload, named events between scripts, modules loaded from disk or a pack, and a debugger with breakpoints, stepping and frames over the Debug Adapter Protocol. The API documents itself, and <code>balaur api</code> prints it.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Scripts and assets",
        title: 'Scenes, assets and packs',
        text: <>Prefabs with an override per path, <code>id://</code> references that survive a rename, import settings in a file beside the picture, sprite sheets with tags and slices, and hot reload for textures, models, fonts and sounds. <code>balaur import</code> reads Aseprite, Tiled and LDtk. A shipped project is a sha256-verified binary pack.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Physics and animation",
        title: 'Rapier in 2D and 3D',
        text: <>The whole of rapier, in both dimensions and behind one script API: bodies with CCD and sleep, joints with motors, limits and breaking, character controllers, raycasts and shape casts, collision and contact-force events, one-way platforms, ray-cast vehicles, and every collider shape rapier has, voxels included and editable while the game runs.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Physics and animation",
        title: 'Rigs and animation',
        text: <>Skeletons in 2D and 3D with the same five modifiers each — <code>look_at</code>, <code>two_bone_ik</code>, <code>fabrik</code>, <code>ccdik</code> and <code>jiggle</code> — GPU skinning, per-vertex deform tracks, morph targets from glTF, a clip retargeted onto another rig through a <code>bone_map</code>, and ragdolls blended back onto the bones by weight.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Gameplay and audio",
        title: 'Widgets, text and the batteries',
        text: <>Fourteen widget kinds with containers, themes, focus and a text field that takes IME input, over cosmic-text, so bidi, CJK breaks and font fallback work. Text in the world as <code>text2d</code> and <code>text3d</code>, and bitmap fonts for a pixel face. Beside them: audio buses and positional audio, input actions with rebinding, save games with migrations, localisation with plurals, and tweens.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Gameplay and audio",
        title: 'Interactivity without a script',
        text: <>Built, bar the rigs. Hover, click, key, action, scroll and resize hooks on any drawn node; <code>states</code> naming a look; typed <code>[variables]</code> on the scene; and <code>[[nodes.bindings]]</code> rows the Events view writes, each one a call a script could make and convertible to one. A <code>when</code> is a comparison over the variables, so the editor reads it and diffs it. Orbit, first-person, third-person and click-to-move rigs as presets are what is left.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Rendering",
        title: 'What draws today',
        text: <>Sprites, atlases and tile maps; 2D lights and shadows; WESL shaders and material assets, including ones that read the screen; bloom, SSAO, SSR and depth of field; ten 3D primitives and six 2D ones, every one a mesh built headless; path assets stroked, extruded, revolved and swept; booleans; a <code>cloner</code>; and textured particles.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Rendering",
        title: 'Post-process materials',
        text: 'A shader of your own on the camera chain, beside the bloom, SSAO and depth of field already there.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Networking",
        title: 'The deterministic core',
        text: <>A fixed 60 Hz step, a digest per tick that CI compares across operating systems, record and replay, and rollback with node ids stable across spawns. Sessions run lockstep over a socket and are recordable, with a faulty transport to test against. HTTP, WebSocket and WebTransport sit behind one <code>Transport</code> trait.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Tools and services",
        title: 'Apple and the platform module',
        text: <>Sign-in, achievements, leaderboards, cloud saves and presence behind one <code>platform</code> module, with Game Center and iCloud behind it, in-app purchase, notifications and URL handling. An <code>[apple]</code> manifest section writes <code>Info.plist</code> and entitlements and signs a macOS app. A store write waits for its tick to settle, so rollback cannot double it.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
      {
        group: "Shipping",
        title: 'Export, the web and the CLI',
        text: <><code>balaur export</code> builds a native or web target and reports what the pack weighs, by section and by largest entry, naming what nothing references; <code>strip</code> drops those, and images, fonts and audio re-encode losslessly or lossily per kind. The editor runs in a browser over IndexedDB. <code>balaur test</code>, a lint script that mirrors CI, and a benchmark suite measured beside Godot's come with it.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/CHANGELOG.md',
      },
    ],
  },
  {
    id: '0.2',
    state: 'building',
    title: "Nothing blocks an ordinary game",
    items: [
      {
        group: "Scripts and assets",
        title: 'Texture import settings',
        text: <>A pixel-art sprite can ask for hard edges today: a settings file sits beside the picture, <code>[import.texture]</code> sets the project's default, and filtering and colour space reach the upload. The editor has an Import tab beside the Inspector for the file the Assets dock has selected, saying for each key whether the value is the file's own, the project's or the engine's. Still to come: repeat, mipmaps, anisotropy and premultiplied alpha, which wait on the renderer exposing a whole sampler, and GPU-compressed textures at export.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-textures.md',
      },
      {
        group: "Physics and animation",
        title: 'Animation blending',
        text: 'Blend trees and state machines that mix clips by weight. A rig is otherwise complete — five modifier kinds in 2D and 3D, retargeting, deform tracks and ragdolls — and what is missing is running two clips at once.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-animation-and-resources.md',
      },
      {
        group: "Physics and animation",
        title: 'Pause, time scale and smooth frames',
        text: <>A pause that holds the game and keeps the menu alive, a <code>process</code> mode per subtree, slow motion and fast forward, motion interpolated between fixed steps so a 144 Hz display sees every frame move, and a tick rate setting.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-time.md',
      },
      {
        group: "Rendering",
        title: 'The 3D look',
        text: <><code>light3d</code> with shadows and light layers, an <code>environment</code> carrying sky, fog, exposure, tonemap and grading, and <code>package::pbr</code> with six texture slots are built; a scene with no light still gets the engine's own. What is left: image-based lighting and SSAO bound, that surface as the built-in a node with no material draws, glTF import keeping factors and maps, glass, mirrors and probes, the finishing passes, and layer stacks.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-3d-rendering.md',
      },
      {
        group: "Networking",
        title: 'Sessions from a script',
        text: 'Host, join and leave from Rune, with a roster whose slots are bound to links; peer, host and headless server roles, the server ordering inputs, verifying every client’s digest and holding the history a late joiner replays from. Rollback runs today between two engines in a Rust test only.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-sessions.md',
      },
      {
        group: "Shipping",
        title: 'Signed releases',
        text: 'The nightly and tagged builds, their runtime templates and the updater exist. What is missing is a download per platform that is signed and notarized, so it opens without a warning.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-release.md#binary-releases',
      },
    ],
  },
  {
    id: '0.3',
    state: 'planned',
    title: "Everything you see",
    items: [
      {
        group: "Editor",
        title: 'Curve editor and onion skin',
        text: 'Tangent handles on keys and ghosted neighbouring frames in the timeline.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-editor.md#curve-editor-and-onion-skin',
      },
      {
        group: "Physics and animation",
        title: 'A sequencer',
        text: 'Cutscenes and cameras on a timeline, with tracks that call something rather than only move it.',
      },
      {
        group: "Rendering",
        title: 'Lit normal-mapped sprites',
        text: <>2D lights and shadows are built. A normal map on <code>sprite</code> needs the light map to carry a direction as well as a colour. Occluding tiles moved to the tile-map plan.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-rendering.md',
      },
      {
        group: "Rendering",
        title: 'Particles in 3D',
        text: <>A <code>particles3d</code> emitter, and in both dimensions: emission from a sphere, box, cone, ring or mesh, randomness, animated sheets, attractors, colliders, trails and sub-emitters, with a compute stepper where the GPU has one.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-particles.md',
      },
      {
        group: "Rendering",
        title: 'Culling and level of detail',
        text: 'Nodes outside the camera skipped and a script that can ask whether one is in view, visibility layers, repeated meshes drawn in one call, and meshes that carry cheaper versions of themselves for distance.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-views-and-culling.md',
      },
      {
        group: "Rendering",
        title: 'Voxels and terrain',
        text: <>Block types in a <code>voxel_set</code>, a greedy chunk mesher with baked ambient occlusion, a grid file that scales past a hand-written one, a Voxels tool that paints in the viewport, <code>balaur import</code> for MagicaVoxel files, and the same mesher over <code>heightfield</code>. Physics is already there — rapier's voxel shape collides without the seam-catching every hand-rolled voxel collider has, and a script can dig into one today. Nothing draws either asset.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-voxels.md',
      },
      {
        group: "Rendering",
        title: 'More than one view',
        text: <>A <code>viewport</code> component: split screen, a camera rendered to a texture any sprite or material can read, picture-in-picture, and a camera with a projection, field of view and clip planes an author sets.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-views-and-culling.md',
      },
      {
        group: "Rendering",
        title: 'Video playback',
        text: 'A movie on a texture with its audio on a bus. Render-side only — a video never feeds simulation state.',
      },
      {
        group: "Rendering",
        title: 'Decals and volumetric fog',
        text: <>A texture projected onto whatever is under it, for a scorch mark, a puddle or a poster, and fog that light shafts through. Neither exists today: <code>environment</code> carries flat fog, and neither pass is in the kiss3d fork's list, so both are ours to write.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-3d-rendering.md',
      },
    ],
  },
  {
    id: '0.4',
    state: 'planned',
    title: "Multiplayer, end to end",
    items: [
      {
        group: "Editor",
        title: 'Network dock and Play as two',
        text: 'Round trip, loss, rollbacks per second and the desync tick per link, and a second instance of the project launched headless and joined over loopback with the fault settings on.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-sessions.md#3-steps',
      },
      {
        group: "Networking",
        title: 'WebTransport in the browser',
        text: 'QUIC runs natively today: reliable streams and unreliable datagrams in one encrypted connection, with datagrams doing the job of raw UDP so the engine never exposes a UDP socket. The browser reaches WebTransport through its own API, which the web build still has to wrap.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#2-transports',
      },
      {
        group: "Networking",
        title: 'Late join, reconnect and host migration',
        text: 'A snapshot on its own stream for whoever arrives late, a grace period that keeps a dropped player’s slot, spectators, the next member taking over when the host vanishes, and bincode instead of JSON on the wire.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-sessions.md#3-steps',
      },
      {
        group: "Networking",
        title: 'State replication and RPC',
        text: <>A <code>replicate</code> key on a node — authority, components, mode — and the server sends deltas off the component schema; RPCs address nodes by stable id.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#3-steps',
      },
      {
        group: "Networking",
        title: 'Client prediction and reconciliation',
        text: 'Your own node moves the frame you press the key, and the server’s correction rewinds that node alone and replays what is still pending, smoothed out over a few frames. Everyone else is drawn a little behind and interpolated.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#hiding-latency',
      },
      {
        group: "Networking",
        title: 'Lag compensation and interest management',
        text: 'The server rewinds to the tick the shooter saw before testing a hit, and sends each client only what it can see, at a send rate below the tick rate.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#3-steps',
      },
      {
        group: "Gamend",
        title: 'A game server per lobby',
        text: 'Gamend launches a headless Balaur when a lobby starts, records its address and certificate hash on the lobby, and players connect to it over QUIC. Gamend keeps the roster; the game keeps the ticks.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#3-steps',
      },
      {
        group: "Gamend",
        title: 'Lobby tokens and rejoin',
        text: 'A lobby-scoped token binds a player to their slot on the game server and lets them back in after a dropped socket. Leaderboard writes come from the server that saw the match, never from the client that claims a score.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#1-design',
      },
      {
        group: "Gamend",
        title: 'The match record',
        text: 'The server’s recording and final digest uploaded as the lobby’s record, so a disputed result is a replay that either matches or names the tick.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#1-design',
      },
      {
        group: "Gamend",
        title: 'Typed bindings for the whole API',
        text: <>One script call per Gamend operation, generated from its OpenAPI document and realtime protobuf, with typed events per channel; <code>rest</code> and <code>push</code> stay for the hooks a game adds.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#engine-side-in-this-repository',
      },
    ],
  },
  {
    id: '0.5',
    state: 'planned',
    title: "A game built without writing a script",
    items: [
      {
        group: "Editor",
        title: 'A graph that writes Rune',
        text: <>Boxes and wires that emit a script. A <code>graphs/*.toml</code> asset writes the <code>.rn</code> beside it, so the digest, the debugger, hot reload and the language server all see ordinary Rune and nothing learns a second language. The palette is generated from the API reference, and the canvas is <code>egui-snarl</code> on the egui the widgets already use.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-authoring-without-code.md',
      },
      {
        group: "Scripts and assets",
        title: 'Scripts you can take',
        text: <>The library dock ships materials, skies and models; this adds scripts. Fifteen short files with <code>exports()</code>, dropped onto a node and tuned in the inspector without opening one: movement in 2D and 3D, orbit, first-person, third-person and click-to-move cameras, follow, patrol, a spawner, a timer, health, a pickup, parallax. A copy lands in the project, so changing one is editing your own file.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-authoring-without-code.md',
      },
      {
        group: "Scripts and assets",
        title: 'Extensions, tier two',
        text: 'Native extensions that add components and systems, and call back into scripts.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-c-api.md#what-tier-1-does-not-do',
      },
      {
        group: "Gameplay and audio",
        title: 'Navigation',
        text: <>A <code>navmesh</code> asset baked from the scene's colliders and tile maps, paths over it through <code>polyanya</code> or over a grid, and <code>agent2d</code> and <code>agent3d</code> that avoid each other and moving obstacles, all on the fixed step and in the digest so a lockstep game runs it on every peer.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-navigation.md',
      },
      {
        group: "Gameplay and audio",
        title: 'Voice in a session',
        text: 'Capture, Opus, a jitter buffer, push-to-talk or voice activity, echo cancellation, and each player heard from where their node stands, on a bus like every other sound. Voice never enters the simulation or the digest.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-voice.md',
      },
      {
        group: "Gameplay and audio",
        title: 'Motion and haptics beyond one pad',
        text: 'Gyro on Switch Pro and Joy-Con, sensor calibration per unit, adaptive triggers and light bars, waveform haptics, motion from a phone or tablet, and gamepads on iOS and Android. A PlayStation pad reports motion and runs both motors today; nothing else does.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-input.md',
      },
      {
        group: "Tools and services",
        title: 'An MCP server',
        text: <><code>balaur mcp</code>: the project, its components and API, <code>check</code>, a headless run and a screenshot as tools an agent drives from Claude Code, Cursor or an IDE, and the running editor's palette behind a second flag. Files are the API, so the editor follows every edit.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-mcp.md',
      },
      {
        group: "Tools and services",
        title: 'A crash report that reproduces itself',
        text: 'The recording, the log and the build id in one file, so a bug report re-runs the session that caused it bit for bit.',
      },
    ],
  },
  {
    id: '0.6',
    state: 'planned',
    title: "A game gets out",
    items: [
      {
        group: "Editor",
        title: 'The editor in a browser',
        text: 'Shipped: the canvas, play and hot reload as on the desktop, a project the browser keeps across a refresh, a folder opened from your machine and taken back out as a zip, and the two builds a tab can finish by itself — a pack and a web bundle. Left: a project kept on a server rather than in one browser, and the native builds, which need a linker no browser has.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-web-editor.md',
      },
      {
        group: "Scripts and assets",
        title: 'Asset streaming',
        text: 'A load that runs off the tick, a scene added to one already running, and an asset dropped when nothing names it — instead of a pack held whole in memory.',
      },
      {
        group: "Tools and services",
        title: 'Accessibility',
        text: 'A screen reader over the widget tree, text scaling, captions and colour-blind-safe defaults. Focus order, localization and rebindable actions are already there.',
      },
      {
        group: "Tools and services",
        title: 'Steam',
        text: <>Sign-in, achievements, leaderboards, Cloud saves, rich presence, the overlay, Workshop, Steam Input and Steam networking — behind the <code>platform</code> module Apple's stores already speak, so one script runs everywhere.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-steam.md',
      },
      {
        group: "Tools and services",
        title: 'Google Play',
        text: 'Play Games Services, Sign in with Google, Play Billing, Play Integrity and Play Asset Delivery on Android, over the same module. An application id, an app bundle and 16 KB page alignment come first: without them Play refuses the upload, store or no store.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-google.md',
      },
      {
        group: "Shipping",
        title: 'One-click deploy',
        text: 'Deploy to the web or to a phone from one command or one button: the game built, signed and put somewhere a player can reach it — a URL, itch.io, a device on the cable, TestFlight, Play or Steam. Today an export leaves a bundle on your disk and stops.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-deploy.md',
      },
      {
        group: "Shipping",
        title: 'A web module that loads in parts',
        text: <>The engine module is most of a web game's download, and nothing inside it loads lazily. Split it, so a game fetches the physics, audio or networking it uses and no more, and let a pack arrive in pieces beside it.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-embed.md',
      },
      {
        group: "Shipping",
        title: 'Embedding on a page',
        text: <>A <code>&lt;balaur-viewer&gt;</code> element and an npm runtime with a React wrapper, a page API to set variables and hear events over the bridge that already exists, a web module sized to what the game uses instead of one that carries everything, a progressive web app option on the export, and image, video and glTF export from the editor.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-embed.md',
      },
      {
        group: "Shipping",
        title: 'Sealed packs and stripped binaries',
        text: 'Today every texture, sound and scene comes out of a shipped game as the original file, scripts as bytecode on native and source on the web, and the binary keeps its symbols. Planned: a stripped binary, bytecode on the web, and a pack sealed with ChaCha20-Poly1305 — a speed bump by name, since the key ships with the game. DRM wrappers, anti-cheat and anti-debugging are not coming; a result that matters is checked by a server.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-protection.md',
      },
    ],
  },
  {
    id: '0.7',
    state: 'planned',
    title: "What a bigger game asks for",
    items: [
      {
        group: "Editor",
        title: 'Semantic tokens, inlay hints and code actions',
        text: <>The rest of the language-server surface: colouring that comes from the compiler rather than a TextMate grammar, an inferred type shown inline beside a binding, and a quick fix offered on a diagnostic. Held back until completion, hover and rename have been used enough to show where the gaps are.</>,
      },
      {
        group: "Scripts and assets",
        title: 'FBX import',
        text: <>The meshes, rigs and clips that never ship as glTF: <code>balaur import</code> over the <code>ufbx</code> crate. Mixamo is the case that forces it, since its downloads are FBX and a rig from there is one the retargeter already handles.</>,
      },
      {
        group: "Physics and animation",
        title: 'Falling sand',
        text: <>A Noita-shaped grid in 2D: sand pours, water flows, lava sets fire to wood and cools to stone, each cell following a rule table in a <code>cell_set</code> rather than a script of its own. It is not physics, so it is not in rapier and not in the default build — a <code>balaur_cells</code> plugin behind a feature that is off, on the fixed step and in the digest. Bodies couple through parry's 2D voxel collider, which <code>collider2d</code> already has and which a script already digs into.</>,
      },
      {
        group: "Physics and animation",
        title: 'Soft bodies, tearing, fluids',
        text: 'Deformable bodies, cloth that tears, liquids and granular materials, built in Rapier and exposed as components.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-physics.md',
      },
      {
        group: "Physics and animation",
        title: 'Root motion',
        text: <>A clip that moves the character instead of sliding under it. The root bone's delta per tick is handed to <code>character2d</code> and <code>character3d</code> rather than written straight to the transform. Blending lands in 0.2; nothing reads a root track yet.</>,
      },
      {
        group: "Networking",
        title: 'WebRTC data channels',
        text: 'Browser peer-to-peer without a relay, behind the same transport trait. Built only once a game needs it; on native, Steam’s Datagram Relay answers the same NAT in the Steam plan.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-networking.md#2-transports',
      },
      {
        group: "Gamend",
        title: 'A WebRTC relay for browsers',
        text: 'The server-side WebRTC peer generalised from hook calls to unordered, unreliable data channels per lobby, so two browsers play through Gamend before the engine has WebTransport in a tab.',
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-gamend.md#2-the-surface',
      },
      {
        group: "Gamend",
        title: 'Skill matchmaking',
        text: 'Ratings, widening bands and leaver refill, on Gamend’s own roadmap. The engine binds whatever the API exposes.',
        plan: 'https://github.com/appsinacup/gamend/blob/main/ROADMAP.md',
      },
      {
        group: "Tools and services",
        title: 'Projects in the cloud',
        text: <>A project on a Gamend account with a version per save, a share link with a role, who else is in the scene and what they have selected, comments pinned to nodes, and editing together — under a lock per scene first, over a CRDT if a team asks.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-collaboration.md',
      },
      {
        group: "Shipping",
        title: 'Per-platform project settings',
        text: <>One <code>project.toml</code>, with <code>[application.android]</code> and its siblings over it: window mode, tick rate, feature set and asset variant per target, resolved when the export runs rather than branched inside a script.</>,
      },
    ],
  },
  {
    id: '0.8',
    state: 'planned',
    title: "The machine a game runs on",
    items: [
      {
        group: "Gameplay and audio",
        title: 'Touch controls a phone needs',
        text: <>Touches are already recorded and replayed; nothing draws a control over them. A <code>touch_button</code> and a <code>touch_stick</code> widget kind, pinch, swipe and long-press recognisers, and a keyboard height a layout can read, so a text field is never hidden under the keyboard.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-input.md',
      },
      {
        group: "Gameplay and audio",
        title: 'A controller-only shell',
        text: <>What a console and a television ask of an interface. Directional focus between widgets rather than <code>focus_next</code> alone, an on-screen keyboard for a text field, safe-area insets applied to the layout instead of only reported by <code>render.safe_area</code>, and button glyphs that follow the pad in hand. The screens asking for it are a console, a tvOS box and an Android TV.</>,
      },
      {
        group: "Shipping",
        title: 'The shell a phone has',
        text: <>Opening a link works on every desktop and in a browser tab, and on neither phone: iOS wants its own open call and Android an intent, so both answer today that they have no opener. Revealing a file in a file manager is a desktop idea and stays one.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-mobile-export.md',
      },
      {
        group: "Shipping",
        title: 'What a phone lends a game',
        text: <>The share sheet, the camera and the photo library, geolocation, biometrics, the clipboard, keep-awake and vibration, behind one module with a desktop answer or an <code>unsupported</code>. These are the gaps a phone game hits first, and the plan for 2D games lists every one of them.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-2d-games.md',
      },
      {
        group: "Shipping",
        title: 'A game on a small machine',
        text: <>An ARM Linux build is already an export target, and nothing has ever run one. A Raspberry Pi wants the GL backend rather than Vulkan, a build that needs no desktop compositor, and a frame budget measured on the board.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-release.md',
      },
      {
        group: "Shipping",
        title: 'Suspend and resume',
        text: <>A phone call, a locked screen, an alt-tab, a console's suspend. Hooks a script answers, audio released and taken back, a save on the way out, and a tick that pauses rather than catching up on return. Nothing in the tree answers a <code>Suspended</code> event today.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-mobile-export.md',
      },
      {
        group: "Shipping",
        title: 'Console export',
        text: 'Switch, PlayStation and Xbox. The export and the pack shape travel; each console’s graphics, input and store layer is an NDA SDK that is none of the crates the engine runs on.',
      },
      {
        group: "Shipping",
        title: 'XR',
        text: <>OpenXR on desktop and standalone headsets, WebXR in a browser, and a phone's own AR through ARKit and ARCore, all behind one seam: stereo views, tracked poses, controller and hand input, and a camera the scene composites onto.</>,
      },
    ],
  },
  {
    id: '0.9',
    state: 'planned',
    title: "A world, not a scene",
    items: [
      {
        group: "Scripts and assets",
        title: 'Procedural noise',
        text: <>Value, perlin, simplex and worley, with fbm over them, seeded the way <code>rng</code> is and computed on the same libm every platform shares. A generated world comes out identical on every machine and inside a replay. The cloner scatters with a private one today, and no script can reach a noise function at all.</>,
      },
      {
        group: "Scripts and assets",
        title: 'A world bigger than a float',
        text: <>Origin rebasing on the fixed step, so a world runs past the precision a 32-bit float has left, with the scene streamed in chunks around the camera. A 64-bit world is not planned. Rebasing lands in the digest, which makes it a decision the tick takes rather than the renderer.</>,
      },
      {
        group: "Gameplay and audio",
        title: 'Sound that fills a room',
        text: <>A bus is a gain tree today. This makes it a graph: reverb, EQ, a compressor and a limiter, music ducked under speech, HRTF so a sound sits at a head rather than between two speakers, and long music streamed instead of decoded whole.</>,
      },
      {
        group: "Rendering",
        title: 'Terrain sculpting and foliage',
        text: <>The <code>heightfield</code> asset and its mesher exist; no tool touches either. Brushes that raise and flatten ground, splat maps for the textures between, and grass and trees scattered by painting rather than placed one at a time.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-voxels.md',
      },
      {
        group: "Rendering",
        title: 'Occlusion culling',
        text: <>What stands behind a wall is drawn today. A software depth rasteriser skips it, over the frustum and distance culling 0.3 brings. It waits on a scene large enough to need it.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-views-and-culling.md',
      },
      {
        group: "Rendering",
        title: 'Global illumination',
        text: <>Light that bounces, computed while the game runs: a screen-space pass, or a field of probes over a signed distance field. Nothing in the renderer's fork does it, and it is the gap named most often against Godot's SDFGI and Unreal's Lumen. Baked lightmaps stay out.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-3d-rendering.md',
      },
    ],
  },
  {
    id: '1.0',
    state: 'planned',
    title: "The editor makes the content",
    items: [
      {
        group: "Editor",
        title: 'More than one window',
        text: <>A second window from the operating system: a dock torn off the editor onto another monitor, or a game drawing on a second display. kiss3d owns the single window there is, which is the same obstacle standing in XR's way, so the two are one piece of work.</>,
      },
      {
        group: "Editor",
        title: 'A shader graph',
        text: <>The same canvas the Rune graph brings, emitting WESL instead of Rune. A material's <code>[params]</code> are already read off the linked shader, so a graph writes a file the pipeline compiles the ordinary way. Every large engine has one, and this one costs a second emitter rather than a second renderer.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-shaders.md',
      },
      {
        group: "Editor",
        title: 'Modelling in the viewport',
        text: <>Every shape the engine draws is already a <code>mesh</code> asset built without a GPU, so a modelling tool edits data that exists rather than a new format. Push and pull a face, bevel an edge, subdivide, unwrap with <code>xatlas</code>. Unreal calls this Modeling Mode and Unity ships ProBuilder; nothing here moves a vertex yet.</>,
      },
      {
        group: "Gameplay and audio",
        title: 'Behaviour trees',
        text: <>A tree asset ticked on the fixed step and inside the digest, its leaves the navigation agents and the binding actions a scene already has, drawn on the same canvas the Rune graph brings. What an enemy does is a script's job today.</>,
      },
      {
        group: "Gameplay and audio",
        title: 'Dialogue',
        text: <>Nothing in the tree holds a conversation. A <code>dialogue</code> plugin over an ink-shaped script: lines addressed by key so the translation table reaches them, the conversation stepped on the fixed tick and inside the digest, a choice raised as an event, and a view that edits the branches.</>,
      },
      {
        group: "Gameplay and audio",
        title: 'Translations as a pipeline',
        text: <>Strings per locale, interpolation, plurals and the system locale are built. The workflow around them is not: importing from a spreadsheet or a gettext <code>.po</code>, swapping an asset per locale, a dock that names every missing key, and pseudolocalisation, which finds the strings nobody wrapped before a translator does.</>,
      },
    ],
  },
  {
    id: '1.1',
    state: 'planned',
    title: "Other people's parts",
    items: [
      {
        group: "Scripts and assets",
        title: 'More importers',
        text: <>Spine and DragonBones, whose 2D skeletal animation is exactly what <code>bone2d</code> and the skinned polygons already draw; layered PSD files; and <code>.blend</code> read by calling Blender, the way Godot does. Aseprite, Tiled, LDtk and glTF are built already.</>,
      },
      {
        group: "Scripts and assets",
        title: 'A package manager',
        text: <>Dependencies named in the project file, added with one command, and pinned in a lockfile that carries a hash per entry. A package is files, plus a native or WebAssembly extension when it needs one, so a plugin, a script library and an art pack all install the same way. A plugin template and a build matrix come with it.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-plugins.md',
      },
      {
        group: "Scripts and assets",
        title: 'Extensions in WebAssembly',
        text: <>A third tier beside the Rust and C ones. A <code>.wasm</code> module runs over <code>wasmtime</code> on a desktop and over the browser's own engine on the web, so an extension is sandboxed and a web build can carry one at all. <code>dlopen</code> reaches neither a browser nor a console.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-plugins.md',
      },
      {
        group: "Tools and services",
        title: 'A library others publish to',
        text: <>The library dock reads one list, the one shipped with the editor. This is the catalogue anyone can publish to, over the same manifest, with a hash per entry and a name saying who wrote it. An asset store made of files.</>,
        plan: 'https://github.com/balaurengine/balaur/blob/main/docs/PLAN-collaboration.md',
      },
    ],
  },
]} />

[Discord](https://discord.gg/v649emcpAu) ·
[Issues](https://github.com/balaurengine/balaur/issues) ·
[Discussions](https://github.com/balaurengine/balaur/discussions)
