---
title: "Architecture: layers, the script seam and plugins"
sidebar_label: "Architecture"
description: "The layers of the Balaur engine: Rune scripts, the language-neutral script seam, the ECS core, and the plugins every subsystem is built as."
image: "/img/social/architecture.png"
sidebar_position: 5
---

# Architecture

The long version is
[ARCHITECTURE.md](https://github.com/balaurengine/balaur/blob/main/ARCHITECTURE.md)
in the engine repository.

## Layers

```mermaid
flowchart TB
  S["scripts: game code, and the editor itself, in .rn files"]
  R["balaur_script_rune: host, hot reload, mod files, debugger"]
  M["balaur_script, the seam: Bindings, ScriptHost, Value. Traits only. No language, no dependencies."]
  D["modules declared once against the seam: engine, scene, node, assets, animation, input, physics, …"]
  subgraph P["plugins"]
    direction LR
    P1["physics (rapier)"] ~~~ P2["render (kiss3d)"] ~~~ P3["audio (rodio)"] ~~~ P4["input (winit)"] ~~~ P5["anim (clip)"] ~~~ P6["your plugin"]
  end
  C["balaur_core: hecs ECS + scene tree + assets + scheduler. Names no scripting language."]
  S --> R --> M --> D --> P --> C
```

Backends depend on core. Core never depends on a backend; the compiler
enforces the direction.

## ECS

- `hecs`: archetype-based, minimal, no application framework.
- Scheduling, hierarchy and resources are Balaur's own.

## Math

- One crate everywhere: `glamx`, glam plus `Pose2`, `Pose3` and `Rot2`, with
  the rest re-exported.
- The same math rapier and kiss3d use. No layer converts between core,
  physics and rendering.
- Pinned to `libm` and `scalar-math` in every build, not only those with
  physics.

## The `Engine` handle

- One cheaply clonable handle over the world, the resources, the command queue
  and the script host.
- Interior mutability, short borrows.
- Rust systems take it every frame; script bindings capture it. Both sides of
  the FFI see one state.
- Single-threaded. Threads can live inside a system without changing the
  facade.

## Scene tree

- A node is an entity with `Name`, `Parent`, `Children` and
  `GlobalTransform`. Its local `Transform` is the `transform` component. A
  node without one sits at its parent.
- Node paths, transform propagation and recursive free are core systems.
- Plugins add their own components to the same entities.

## The frame

```mermaid
flowchart LR
  A["First"] --> B["PreUpdate<br/>reload pump"] --> C["Update<br/>scripts, animation"]
  C --> D["FixedUpdate<br/>fixed_update, physics"] --> E["PostUpdate<br/>audio"]
  E --> F["SceneSync<br/>transforms"] --> G["Render"] --> H["Last<br/>deferred destruction"]
```

| Rule | |
| --- | --- |
| Fixed step | `FixedUpdate` drains one accumulator into whole 60 Hz steps, zero to four a frame |
| Never the frame time | The simulation never sees the measured frame time |
| Deferred structure | A script's spawn or free waits for `Last` |

## The script seam

- `balaur_script` is traits and one neutral `Value`: no language, no
  dependencies.
- A subsystem declares its bindings once against the seam and never names a
  language.
- A backend implements the host trait and consumes those declarations.
- Rune is the shipped backend. A second language is one crate; physics,
  input, render, audio and ui do not change.

## Plugins

- Wrapping a Rust crate for scripting is one call per entry point, with
  argument and return conversions inferred.
- One component registration gives the scene key, the runtime API and editor
  support.
- `balaur_physics` is the reference implementation.
- One `Plugin` trait covers both shipping modes: linked in behind a cargo
  feature, or built as a shared library and loaded at run time. See
  [Plugins](./manual/plugins.mdx).

## The workspace

| Crate | Role |
| --- | --- |
| `balaur_script` | The scripting seam: traits and a neutral value type; no language, no dependencies |
| `balaur_core` | ECS world (hecs), scene tree, scheduler, plugin API, skeletons, glTF and mesh assets, digests, replay, snapshots, pack export |
| `balaur_script_rune` | The Rune backend: host, hot reload, `mod` files, the debugger, export |
| `balaur_physics` | Rapier plugin; the reference for wrapping a Rust crate for scripting |
| `balaur_render` | Renderable components (shapes, sprites, skinned polygons, meshes) + `render` module; kiss3d/wgpu backend behind a feature flag |
| `balaur_anim` | Animation clips, the pure sampler, tweens, easing, rig modifiers; the `animation` module |
| `balaur_audio` | rodio-backed `audio` module |
| `balaur_input` | Backend-agnostic input snapshot + `input` module |
| `balaur_http` | `http` for scripts, delivered into the simulation per tick |
| `balaur_websocket` | `websocket` for scripts, and a `Transport` for sessions |
| `balaur_webtransport` | QUIC behind the same `Transport`, with native datagrams |
| `balaur_gamend` | The Gamend backend (`gamend` module): login, REST, realtime, server hooks |
| `balaur_platform` | The `platform` module and the seam a store plugs into: sign-in, achievements, leaderboards, cloud saves |
| `balaur_apple` | Game Center, iCloud and Sign in with Apple behind that seam, plus `apple` for purchases, notifications and opened URLs |
| `balaur_plugin` | The `Plugin` trait a module or run-time extension implements, plus the loader |
| `balaur_ui` | Immediate-mode egui API (`ui` module): panels, widgets, script-defined themes |
| `balaur` | Batteries-included facade: `standard_app`, `boot_project`, `boot_pack` |
| `balaur_export` | Export as a library: packs, fused executables, and the Apple and Android bundles; the editor and the CLI both use it |
| `balaur_cli` | The `balaur` binary: `new`, `run`, `export`, `play`, `edit`, `import`, `replay`, `api` |
| `balaur_bench` | Headless benchmarks: where a frame's time goes, and what scripting costs |
